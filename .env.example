# Chemin vers le vault Obsidian (absolu ou relatif au moteur).
# Le script `npm run link:vault` crée la junction src/content/docs -> $VAULT_PATH.
# Exemple sibling : ../ia-on-prem-vault
VAULT_PATH=../ia-on-prem-vault
